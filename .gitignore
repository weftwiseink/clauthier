.vscode/*
.lace/
build/
node_modules/

# CC rules are loaded via @-imports in CLAUDE.md (source repo)
# or injected by SessionStart hook (external installs).
# .claude/rules/ files are generated artifacts, not canonical sources.
.claude/rules/

# OC plugin artifacts (generated by npm install / build:cdocs)
.opencode/skills/
.opencode/plugins/
